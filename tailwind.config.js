/** @type {import('tailwindcss').Config} */
module.exports = {
  darkMode: "class", // must be 'class' for toggling
  content: ["./src/**/*.{js,jsx,ts,tsx}"],
  theme: { extend: {} },
  plugins: [],
};
